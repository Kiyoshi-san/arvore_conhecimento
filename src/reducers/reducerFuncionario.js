export default function(state = {}, action) {
    switch (action.type) {
        case SET_FIELD:
            action.SET_FIELD
            action.value
            break;
        default:
            return
    }
}